---
import Base from "../layouts/Base.astro";
import Home from "../components/Home.astro";
import { useContent } from "../lib/content";

const t = useContent("es");
---

<Base
	locale="es"
	path="/"
	title={t.meta.title}
	description={t.meta.description}
>
	<Home locale="es" />
</Base>
