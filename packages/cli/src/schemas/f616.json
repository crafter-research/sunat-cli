{
	"command": "f616 declare",
	"description": "File Formulario Virtual 616 monthly tax declaration for 4ta categoria workers",
	"fields": {
		"periodo": {
			"type": "string",
			"required": true,
			"format": "YYYY-MM",
			"description": "Tax period (e.g. 2025-10 for October 2025)"
		},
		"ingresoPEN": {
			"type": "number",
			"required": false,
			"min": 0,
			"description": "Ignored. SUNAT prefills the income from your registered RHE and the CLI does not overwrite it. Verify the prefilled amount in the portal before submitting."
		},
		"retenciones": {
			"type": "number",
			"default": 0,
			"description": "Ignored, same reason as ingresoPEN. SUNAT applies its own withholding records."
		},
		"telefono": {
			"type": "string",
			"required": false,
			"description": "Contact phone written into the form."
		},
		"profesion": {
			"type": "string",
			"required": false,
			"description": "Profession selected in the form."
		}
	},
	"flags": {
		"--dry-run": "Preview the declaration without submitting to SUNAT",
		"--output json": "Machine-readable JSON output",
		"--batch --months <range>": "Declare multiple months (e.g. '2025-03..2026-02')"
	},
	"computed": {
		"pagoACuenta": "Computed by SUNAT from its own prefilled figures, not by this CLI."
	},
	"auth": "Requires Nueva Plataforma session. Run 'sunat-cli login --nueva-plataforma' first.",
	"portal": "Nueva Plataforma (e-menu.sunat.gob.pe/cl-ti-itmenu2/) — requires reCAPTCHA v2 (one-time)"
}
